## Blockchain Backend (NeuroBlock Rehab)

This service stores cryptographic hashes and timestamps of rehab session summaries to ensure tamper-proof, privacy-preserving verification of patient recovery.

Endpoints:

- POST /record
- GET /history
